<h1>商品一覧</h1>
<% @items.each do |item| %>
      <div class="col-md-5 pt-4">
        <p>
          <%= link_to item_path(item.id) do %>
          <%= item.name %></p>
          <% end %>
        <p><%= item.introduction %></p>
        <p>￥<%= item.price %>  </p>
<% end %>