<% if shop.favorited_by?(current_customer) %>
    <p>
      <%= link_to shop_favorites_path(shop), method: :delete, remote: true do %>
        ❤<%= shop.favorites.count %> nice sauna
      <% end %>
    </p>
    <% else %>
    <p>
      <%= link_to shop_favorites_path(shop), method: :post, remote: true do %>
        ♡<%= shop.favorites.count %> nice sauna
      <% end %>
    </p>
  <% end %>